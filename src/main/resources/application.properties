# DataSource settings: set here your own configurations for the database
# connection. In this example we have "netgloo_blog" as database name and
# "root" as username and password.
spring.datasource.url = jdbc:mysql://localhost:3306/demo
spring.datasource.username = root
spring.datasource.password =

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.session.store-type = none

spring.data.rest.basePath=/data
spring.data.rest.baseUri=/data

com.frakseno.neibothoodDataUrl=https://data.baltimorecity.gov/resource/jmja-s2wq.json?$order=neighborhood&$group=neighborhood&$select=neighborhood,count(name)
com.frakseno.museumDataUrl=https://data.baltimorecity.gov/resource/jmja-s2wq.json
com.frakseno.restaurantDataUrl=https://data.baltimorecity.gov/resource/abuv-d2r2.json
com.frakseno.googleApiKey=AIzaSyDXDiyEtqca-oa9ap5V0EJ8uiyYbtT6UKU